name: Tests
on: push
jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          rep: discrete-mathematics
      - run: |
              ls
              git branch
              git branch -r
