name: Tests
on: push
jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run:  |
              git branch
              git branch -r
